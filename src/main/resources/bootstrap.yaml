
spring:
   profiles:
     include: dev,prod
   application:
    name: config-server
   cloud:
     config:
       server:
         #native:
         #  search-locations: file:///C:/Users/.....
        git.:
         uri: https://github.com/macsharker6/myProp
         search-paths: DLL_*
         force-pull: true
         proxy:
           https:
             host: PROXY_HOST
             password: PROXY_PWD
             port: 'PORT'
             username: PROXY_USR
